motivação: ter que digitar usuário e senha toda vez que fizer um push ou clone
a chave ssh e pra que serve
gerando uma chave ssh
arquivos gerados: rsa e rsa.pub
adicionando a chave à conta do GitHub
testando a conexão ssh
possível mudança na url do repositório: https -> ssh

obs:

se existir a chave?
se criar uma chave com o mesmo nome de arquivo?
